= div_for destination do
  .destination
    %strong= destination.name
    .actions
      = link_to 'edit', edit_trip_destination_path(@trip, destination)
      = link_to 'delete', trip_destination_path(@trip, destination), method: :delete, remote: true
